\supplementarysetup
This Supplementary Information provides the complete algorithm, formal locking guarantees, computational complexity, archived and secondary analyses, one-time post-freeze AMP-AD evaluation details, a coherent CGGA benchmark, a reduced-budget saved-bank nested sensitivity, a controlled candidate-bank locking simulation, a fully nested TCGA GBM/LGG analysis, a private radiomics internal demonstration, and machine-readable audit correspondence. The package preserves the original algorithms as legacy-compatible modes, and archived results are not relabeled as updated-method results. Complete candidate- and metric-level audits are supplied as machine-readable CSV files with analysis provenance. \Crefrange{fig:supp20}{fig:supp21} provide descriptive tuned-evaluator sensitivity and computational provenance and are not used for a core claim.


\section{Supplementary Methods}

\subsection{Complete WrapEvoFS algorithm}

\input{tables/supplementary_algorithm_s1.tex}

The algorithm makes the leakage boundary operational: held-out data are absent from search, candidate rescoring, eligibility, representative locking, and audit export. The main text gives the scientific definitions; the following sections provide the formal and engineering details. The executable v0.2.0 wrapper, RFECV, genetic-operator, evaluator, locking, and randomness specifications are consolidated in \cref{tab:supp23}; archived dataset-level metric and cap overrides remain in \cref{tab:supp26}.

\subsection{Stochastic-agreement summary}

For five saved GA runs, Nogueira stability was computed from the run-by-candidate binary selection matrix as one minus the mean observed per-feature sample variance divided by the Bernoulli variance expected from the mean selected proportion \citep{ref36}. The candidate-universe size was the branch-specific Direct set size. With only five runs, these estimates are noisy and characterize stochastic agreement on one fixed development sample rather than stability under participant resampling.

\input{regret_locking_formalization/REGRET_LOCKING_PROPOSITIONS.tex}

\subsection{Private radiomics internal demonstration}

The private radiomics analysis began from two frozen supervised matrices with identical participant order, outcomes, and shared feature values: a full 1,781-feature T1ce tumor-core space and an order-preserving 1,346-feature subset. The subset definition was frozen label-independently in a deterministic 15-participant pilot using features extracted from original, 1-mm-eroded, and 1-mm-dilated automatic tumor-core masks. Retention required ICC(2,1) at least 0.85 and median relative range at most 0.25. Outcome linkage occurred only after imaging selection, conversion, segmentation QC, configuration freezing, extraction, and stability filtering. The transferred modeling bundle did not include pilot-level perturbation measurements, scanner/site covariates, repeat segmentations, masks, or patient-level QC records; those upstream checks could therefore not be independently recomputed.

Both feature spaces used the same stratified 70:30 split (random state 42): 137 development participants and 60 held-out participants. For each space, SVM-L1, XGBoost, and Boruta-RF generated Direct candidates; five-fold RFECV supplied a soft target; and seeds 42--46 each completed a 50-generation, 50-individual GA using five-fold ROC-AUROC, the updated untruncated shifted-linear objective, and a target-deviation penalty of 0.015. All 30 GA completion records stated that held-out data were unused and passed checksum, mask-hash, generation-count, and finite-score audits. The five run-best masks in each of six banks were then rescored by one fixed five-fold development-only random-forest evaluator and locked using the strict absolute regret constraint $\delta=0.01$. The frozen package policy retained exact duplicate masks as multiple voting candidates while recording their common scientific feature set and source-run provenance separately.

All six development locks were written before held-out evaluation. A fixed 500-tree random forest was then fitted on the entire development partition for Direct, RFECV, and locked feature sets and applied once to the held-out partition. AUROC and AUPRC intervals and paired AUROC differences used 2,000 class-stratified bootstrap resamples. The second feature space reuses the same participants, outcomes, and split, so it is a feature-definition sensitivity analysis rather than an external validation. Five-seed agreement is likewise stochastic agreement on one development sample, not participant-resampling or biomarker stability. Participant-level predictions are retained outside the manuscript package; only non-identifying aggregate summaries and locking audits are distributed.

\subsection{AMP-AD post-freeze evaluation and Rush source recovery}

The 24 updated AMP-AD development locks were enumerated in a frozen inventory containing the held-out center, branch, cap, selected run, selected feature list, fixed locking score, empirical regret, eligible-pool size, tie path, and source paths. Before any held-out-center file was read, the evaluation protocol fixed the primary and secondary metrics, final random-forest estimator, run-derived seeds, archived comparators, 2,000-resample paired bootstrap, and reporting rule. The protocol and inventory hashes were then checked at evaluation entry, and a completion sentinel prevented a second execution. All 24 conditions were reported irrespective of direction. The protocol SHA-256 was:
\begin{center}\scriptsize\ttfamily 7bc7b27e00f7337654b6f81c1448a831ce3979129e68d14666389445d3ec93b5\end{center}
This temporal freeze prevents held-out-guided selection within the analysis but does not make the evaluation prospective clinical validation or create 24 independent cohorts.

The authoritative Rush source was a Google Drive results folder rather than an original ZIP archive. All 30 run-best feature CSV files, the run-level summary, and the locking summary were recovered with their Drive file IDs and URLs. A local provenance ZIP was reconstructed without claiming it to be the original archive. Current strict regret-constrained locking reproduced all six archived Rush selected runs and all six masks used by the frozen held-out evaluation; maximum selected empirical regret was zero, no exact duplicate masks occurred, and no selection reached the stable-hash tie stage. Recovery completed the updated-objective cross-lock and duplicate audits for all 24 AMP-AD conditions.

\subsection{CGGA coherent benchmark and nested saved-bank re-locking}

For the coherent benchmark, the raw CGGA expression and clinical files were reconstructed using the archived fixed split and train-fitted missingness, minimum-positive imputation, log transformation, and zero-variance filtering. Participant order, labels, and every value in the archived compact development and held-out matrices were required to match before analysis continued. Six existing development-derived signatures per branch were compared: RFECV-only, size-matched Elastic Net, highest fixed development-CV candidate, legacy top-three medoid, unrestricted full-bank medoid, and regret-constrained medoid. Every signature used the same 500-tree random forest and the same 92-sample held-out set. The 2,000 class-stratified bootstrap samples were common across methods, enabling paired regret-constrained-medoid contrasts. No feature-selection stage was rerun.

The nested saved-bank sensitivity reused five outer-fold candidate banks from an earlier development-only CGGA analysis that had been excluded from the primary release because its CPU search budget was reduced to 20 individuals and 12 generations. Each bank was re-locked with the regret-constrained medoid at absolute \(\delta=0.01\) and the canonical tie-break; the GA, Direct selection, RFECV, candidate score computation, and original 92-sample held-out evaluation were not rerun. A fixed 500-tree random forest was fitted within each outer training fold using the newly selected existing candidate, and OOF predictions were summarized across 214 development participants. The regret-constrained medoid changed all five selections and reduced OOF AUROC relative to the legacy top-three medoid. This result is retained as a limitation-oriented sensitivity and does not replace the main fixed-split CGGA experiment.

\subsection{Fully nested TCGA GBM/LGG analysis}

The TCGA analysis began from a provider-organized common-participant workbook with the following SHA-256:
\begin{center}\scriptsize\ttfamily 31ad68d866836cc786cc7c6421d28b7e4359c7a2b1959753618f5b469aecfafe\end{center}
The included sheets contained 491 participants, 20,118 processed RNA-seq predictors, 5,000 cleaned SCNV predictors, and four historical histological labels. Each predictor retained an \texttt{RNA::} or \texttt{SCNV::} prefix. Clinical columns, RPPA, and the uncleaned SCNV sheet were excluded. The source values were not transformed during extraction. The resulting participant-by-feature matrix and labels were checksum-locked before modeling.

Two shuffled stratified five-fold outer-CV repeats used random states 20260810 and 20260811. Median imputation, zero-variance filtering, branch-specific Direct screening, deterministic post-Direct capping at 100 features, RFECV, GA search, fixed candidate rescoring, and locking were fitted exclusively within each outer-training fold. The three Direct branches were SVM-L1, XGBoost, and Boruta-RF. RFECV used five-fold macro one-vs-rest AUROC and branch caps of 15, 20, and 10, respectively. Each of the 30 repeat--fold--branch banks comprised five GA runs with seeds 42--46, 50 individuals, 50 generations, the untruncated shifted-linear objective, and a target-deviation penalty of 0.015. The locking evaluator used a 500-tree random forest, five fixed outer-training folds, larger-is-better macro one-vs-rest AUROC, strict absolute eligibility at \(\delta=0.01\), and the canonical content-derived tie-break. Exact duplicates would have retained multiplicity as voting candidates with source-run provenance stored separately; no within-bank duplicate masks occurred.

The complete candidate audit in \cref{fig:supp16} plots all 150 candidates against empirical regret and Jaccard distance from the canonical highest-score mask. Independent reconstruction verified every highest-score mask, eligible pool, selected mask, selected regret, pairwise Jaccard, and mean Jaccard to numerical precision. All 30 selected regrets met the tolerance. Six pools were singletons. All five two-candidate pools were structural mean-Jaccard ties and retained the highest-score mask. The regret-constrained medoid differed from the highest-score mask in 11 banks, all with at least three eligible candidates and positive mean-Jaccard gain.

Candidate agreement and participant-partition sensitivity are expanded in \cref{fig:supp17}. Nogueira stability was computed separately within each outer-training bank on that bank's fixed post-Direct universe; panel a reports the unweighted mean of the five fold-level estimates for each repeat--branch combination rather than a pooled 25-mask coefficient. A small negative finite-sample value is valid. Outer-fold pairwise similarities share folds and are dependent, so their distributions are descriptive. Cross-branch Jaccard was not computed because the branch-specific candidate universes differ. Cardinality similarity was defined as \(\min(k_1,k_2)/\max(k_1,k_2)\). SCNV composition largely reflects the upstream Direct universes and is not interpreted as biological modality preference.

Six signatures were evaluated within each repeat--fold--branch condition: Direct, RFECV-only, highest-score locking, legacy top-three medoid, unrestricted medoid, and regret-constrained medoid. All used the same 500-tree random forest and common deterministic seed \(50000 + 1000\times\text{repeat} + 100\times\text{fold} + 10\times\text{branch index}\). A guarded common-seed refresh reused only the already selected feature sets and reran no GA, Direct, RFECV, or adaptive selection. All 150 GA jobs remained unchanged; 180 final method fits produced 420 expected outer-prediction rows, and all completion and prediction checksums passed. The current protocol SHA-256 was \texttt{25f9f196b59b017f8edd5445f8a4d4fc467d399703706662c77a69146e90dad7}. Repeated-OOF metrics and the complete comparator audit are shown in \cref{fig:supp18}. Confidence intervals and paired differences used 2,000 participant-clustered, class-stratified bootstrap resamples that retained both repeat-specific OOF predictions for each sampled participant. The analysis estimates repeated internal OOF performance and participant-partition sensitivity in one TCGA-derived cohort; it does not establish external validity, modern WHO diagnostic validity, biomarker stability, causal biology, clinical utility, or global predictive superiority.

\subsection{Controlled candidate-bank locking simulation}

The controlled simulation isolated finite-bank locking under a hidden utility that was unavailable to each locking rule. The canonical universe contained $p=1000$ features arranged as 20 latent signal groups with one canonical feature and four proxies per group. Candidate sets contained approximately 20 features. For candidate set $F$, hidden utility was
\begin{equation}
T(F)=\operatorname{clip}\left(0.84+0.05G(F)-0.0005I(F)-0.00025\left||F|-20\right|,\ 0.75,\ 0.90\right),
\end{equation}
where $G(F)$ is mean group-aware recovery and $I(F)$ is the number of selected features outside the 20 signal groups. A selected canonical member received group credit 1; proxies received credit 0, 0.6, or 0.9 under the none, moderate, or strong proxy conditions. The observed larger-is-better locking score was $L=T+\epsilon$, where $\epsilon$ followed the prespecified Gaussian distribution in the primary experiment and alternative Laplace, standardized Student-$t_3$, or quantized-Gaussian distributions in sensitivity analyses. Hidden $T$ is a stylized utility and is never interpreted as AUROC.

The primary grid crossed three candidate topologies (aligned, competing, and misleading), three calibrated heterogeneity levels, three proxy strengths, and six score-noise ratios (0, 0.25, 0.5, 1, 2, and 4). Each of the 162 primary scenarios contained 10,000 replicate-indexed banks with $R=5$, absolute $\delta=0.01$, Gaussian score noise, and no forced duplicates. In aligned banks the candidates shared the same canonical prototype before mutation; competing banks introduced a second proxy-based prototype; and misleading banks gave 80\% of candidates a systematically lower-utility majority prototype. Prespecified sensitivities varied $R$ over 3, 10, 20, 50, and 100 with a matched $R=5$ reference; varied $\delta$ over 0, 0.005, 0.01, and 0.02; replaced Gaussian score noise; and introduced duplicate rates 0.1 and 0.3. The complete workload comprised 513 scenarios and 4,995,000 banks. No participant-level data, classifier, preprocessing operation, RFECV, Direct selector, or GA was simulated or fitted.

The four rules were highest score, legacy top-three Jaccard medoid, unrestricted full-bank Jaccard medoid, and regret-constrained medoid. Oracle regret was $\max_rT_r-T_{\widehat r}$, empirical regret was $\max_rL_r-L_{\widehat r}$, score optimism was $L_{\widehat r}-T_{\widehat r}$, and substantive failure was prespecified as oracle regret greater than 0.02. Full-bank mean Jaccard quantified representativeness. Primary results are equal-scenario means: the scenario grid is not a population prior. The regret-constrained medoid had zero configured empirical-regret violation in all 4,995,000 banks. Exact duplicate masks retained multiplicity as voting candidates, and source-run provenance remained post-selection metadata. The stable hash was evaluated only as the final deterministic ordering stage; it was not a scientific score.

Randomness used counter-based SplitMix64 streams keyed to the frozen master seed, scenario stream, and replicate identifier, making candidate generation independent of chunk boundaries, execution order, restart history, and worker count. All 1,296 completed chunks passed checksum and finite-value validation, and 18 prespecified deterministic regenerations matched exactly. The frozen scientific protocol SHA-256 was \texttt{6bb91ec337c331aa4b69646f5166831503405d2721a40d16be75718179418d6f}. The protocol's engineering list named chunk sizes of 1,000 or larger; the $R=100$ sensitivity used chunks of 250 to limit peak memory. This execution-level deviation changed no scientific parameter, and all chunk-independence and integrity checks passed. Peak resident memory was 313.7 MiB. Figure and table source CSVs, their hashes, and the extraction provenance accompany the manuscript. Results characterize locking behavior under the specified utility and do not establish predictive superiority, unbiased generalization, external validity, participant-resampling stability, biomarker stability, clinical utility, or globally optimal feature selection.

\subsection{Computational complexity and implementation correspondence}

Let $R$ be the total retained-candidate count, $E$ the regret-eligible count, $p$ the common canonical-universe size, $s$ the average selected-set size, and $w$ a packed machine-word width. Finding $L_{\mathrm{best}}$, calculating all regrets, and forming $E_\delta$ each require $O(R)$ time; stored regrets and eligible indices require $O(R)$ and $O(E)$ memory, respectively, although a score-only pass can use constant working memory. Dense canonical masks and their authoritative SHA-256 hashes require $O(Rp)$ time and $O(Rp)$ retained memory, or $O(p)$ mask scratch plus $O(R)$ digests if streamed. If no universe is supplied, deriving the package's sorted union adds $O(Rs+p\log p)$ time. A supplied common universe is preferred because it makes the scientific mask domain explicit.

A dense bit-vector Jaccard comparison costs $O(p)$ time, a sparse hash-set comparison costs expected $O(s)$ time (or $O(|F_r|+|F_s|)$ for sorted-index merging), and a packed-bit comparison costs $O(\lceil p/w\rceil)$ word operations with $O(E\lceil p/w\rceil)$ packed-mask storage. Thus all eligible comparisons cost $O(E^2c_J)$, where $c_J$ is the representation-specific pair cost. Retaining the complete ordered $E\times E$ matrix requires $O(E^2)$ similarity memory. Streaming each unordered pair into $E$ running sums has the same asymptotic time but only $O(E)$ accumulator memory beyond the masks; a complete pairwise audit must then be streamed to storage or omitted.

After hashes exist, deterministic tie-breaking costs $O(E)$ with a best-key scan or $O(E\log E)$ by sorting. Canonically ordering the complete audit costs $O(R\log R)$. Audit serialization requires $O(Rp+E^2)$ time and output size when canonical masks and the full pairwise table are retained. The package's dense-mask, sparse-Jaccard, complete-audit implementation therefore has expected time $O(Rp+E^2s+R\log R)$ and memory $O(Rp+E^2)$. With the manuscript's $R=5$, there are at most 10 unordered off-diagonal comparisons or 25 ordered entries including the diagonal. This small use case does not establish scalability to large banks. Detailed operation-level bounds are supplied in \path{regret_locking_formalization/REGRET_LOCKING_COMPLEXITY.md}; implementation correspondence is summarized in \cref{tab:supp24}.


\section{Supplementary Figures}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=180mm]{figures/figure_s17.png}}
\caption{Original-configuration zero-truncation diagnostics. a) Absolute target-size deviation versus the fraction of zero run-best fitness values; the star marks the AMP-AD Rush/SVM-L1/Small failure mode. b) Absolute target-size deviation versus all-zero generations. c) Configurations with a flattening warning by dataset.}
\label{fig:supp1}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=180mm]{figures/figure_s18.pdf}}
\caption{Complete development-only original-versus-updated AMP-AD comparison across 24 center--branch--cap conditions and 120 full GPU GA runs. a) Absolute target deviation. b) All-zero-generation diagnostic. c) Aggregates by center and branch. Overall values were 216 versus 137 and 673 versus 333, respectively; detailed robust summaries are in \cref{tab:supp2}. Held-out outcomes were not accessed.}
\label{fig:supp2}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=180mm]{figures/figure_s15.png}}
\caption{Development-only compression and regret. a) Compression relative to each Direct candidate set versus development-CV regret for the highest-development-CV run, original top-three medoid, all-run medoid, and regret-constrained medoid with absolute tolerance 0.01. The dashed line marks the 0.01 regret bound. b) Median and interquartile-range development-CV regret by locking rule. Held-out outcomes were not used to define any displayed rule.}
\label{fig:supp3}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=180mm]{figures/figure_s16.pdf}}
\caption{Development-only strict locking-rule sensitivity. a) Mean eligible-pool size. b) Selected-candidate mean eligible-pool Jaccard for nonsingleton pools; singleton values are undefined and shown as missing. c) Mean selected empirical development-CV regret across absolute tolerances 0, 0.005, 0.01, and 0.02. No tolerance-expanding fallback was used.}
\label{fig:supp4}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=180mm]{figures/figure_s1.png}}
\caption{ADNI held-out performance and paired differences. a) Balanced accuracy, b) macro F1, and c) macro one-vs-rest AUROC for the first-stage Direct signatures (open symbols) and WrapEvoFS locked signatures (filled symbols), shown by branch with 95\% bootstrap confidence intervals. d) Paired WrapEvoFS-minus-first-stage differences from 5,000 class-stratified paired bootstrap resamples. All nine paired 95\% confidence intervals include zero; equivalence and non-inferiority were not tested.}
\label{fig:supp5}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=180mm]{figures/figure_s2.png}}
\caption{ADNI multiclass held-out diagnostics. a) Row-normalized confusion matrices for the first-stage Direct and WrapEvoFS locked signatures in the SVM-L1, XGBoost, and Boruta-RF branches. b) Class-specific recall for CN, MCI, and dementia, with open and filled symbols denoting first-stage and WrapEvoFS signatures. c) Macro one-vs-rest ROC curves and AUROC values for the same six models.}
\label{fig:supp6}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=180mm]{figures/figure_s3.png}}
\caption{ADNI signature composition and cross-branch overlap. a) Proteomic and metabolomic feature counts for first-stage and WrapEvoFS signatures in each branch. b) Pairwise Jaccard similarity among the three locked signatures. c) Exclusive and shared locked-signature feature counts shown with an intersection-membership matrix. The displayed sets are descriptive and are not stable biomarker claims.}
\label{fig:supp7}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=180mm]{figures/figure_s12.png}}
\caption{AMP-AD paired pooled held-out differences across RFECV cap settings. a) Macro one-vs-rest AUROC, b) balanced accuracy, and c) macro F1. Filled circles show locked-minus-Direct differences and open triangles show locked-minus-RFECV-only differences; horizontal intervals are 95\% paired confidence intervals. Forty of 54 exported intervals include zero; directions vary by branch, cap, metric, and comparator, and resampling conditions on four fitted center-specific models.}
\label{fig:supp8}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=180mm]{figures/figure_s13.png}}
\caption{AMP-AD cross-center recurrence and modality composition. a) Mean pairwise Jaccard similarity across centers for RFECV-only and locked-medoid signatures in each branch and cap condition. b) Mean locked-signature composition split between frontal proteomics and metabolomics for the same conditions. Cross-center overlap quantifies recurrence across center-specific training sets, not biological replication.}
\label{fig:supp9}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=170mm]{figures/figure_s21.pdf}}
\caption{AMP-AD one-time post-freeze held-out evaluation of the corrected objective with regret-constrained medoid locking. a) Macro one-vs-rest AUROC for 24 frozen center--branch--cap signatures; colors identify branches, open circles denote the Small cap, and filled squares denote the Reference cap. b) Pooled regret-constrained-medoid-minus-RFECV-only and c) regret-constrained-medoid-minus-legacy-top-three-medoid macro-AUROC differences with 95\% intervals from 2,000 common resamples stratified by held-out center and true class (seed 42). All masks, parameters, and the protocol hash were frozen before held-out outcomes were loaded; evaluation triggered no reselection or parameter changes. Four of six intervals in b) and five of six in c) include zero.}
\label{fig:supp10}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=180mm]{figures/figure_s7.png}}
\caption{Complete CGGA held-out comparison. a) AUROC, b) AUPRC, and c) balanced accuracy for the Direct signatures (open symbols) and locked full-medoid signatures (filled symbols) in the SVM-L1, XGBoost, and Boruta-RF branches; horizontal intervals are the corresponding held-out uncertainty intervals.}
\label{fig:supp11}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=180mm]{figures/figure_s8_a.png}}
\caption{CGGA run-level stochastic agreement. a) Five-run Jaccard matrices without and with target-size guidance; gold outlines mark the locked medoid run.}
\label{fig:supp12}
\end{figure}

\begin{figure}[p]
\ContinuedFloat
\centering
\makebox[\linewidth][c]{\includegraphics[width=180mm]{figures/figure_s8_bc.png}}
\caption[]{CGGA run-level stochastic agreement (continued). b) Run-level feature counts without and with guidance. c) Target-guided selection frequencies; filled dots mark the locked signature.}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=180mm]{figures/figure_s19_ab.png}}
\caption{CGGA original-configuration mechanism and seed-agreement ablation. a) Held-out AUROC relative to Direct for RFECV-only and selected-run variants. b) Five-run feature counts and mean pairwise Jaccard with and without target-size guidance. c) Paired locked-minus-RFECV-only differences. d) Raw Jaccard and chance-corrected Nogueira agreement.}
\label{fig:supp13}
\end{figure}

\begin{figure}[p]
\ContinuedFloat
\centering
\textit{Supplementary Figure S13 (continued).}\par\medskip
\makebox[\linewidth][c]{\includegraphics[width=180mm]{figures/figure_s19_cd.png}}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=170mm]{figures/figure_s22.pdf}}
\caption{CGGA coherent same-split, same-estimator benchmark. a) Feature counts and b) held-out AUROC with 95\% class-stratified bootstrap intervals for six development-derived signatures in each branch. c) Paired regret-constrained-medoid-minus-comparator AUROC differences from 2,000 common resamples (seed 42); all 15 intervals include zero. All signatures were evaluated with the same fixed 500-tree random forest on the same 92-sample held-out partition. Held-out outcomes did not determine any feature set, locking rule, or comparator.}
\label{fig:supp14}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=170mm]{figures/figure_s23.pdf}}
\caption{CGGA post hoc saved-bank nested sensitivity. a) Selected run IDs under the legacy top-three medoid and regret-constrained medoid across five outer folds. b) Corresponding outer-fold AUROC. c) Overall OOF AUROC, AUPRC, and balanced accuracy with 95\% class-stratified bootstrap intervals. The regret-constrained medoid changed all five selected runs; OOF AUROC was 0.692 under the legacy top-three medoid and 0.667 under the regret-constrained medoid. The source banks used a reduced historical CPU search budget (20 individuals, 12 generations, five runs), no GA was rerun, and the original 92-sample held-out partition was not accessed.}
\label{fig:supp15}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=170mm]{figures/figure_s25_repeat_1.pdf}}
\caption{Complete candidate-level audit of fully nested TCGA locking. Pages 1 and 2 show outer repeats 1 and 2, respectively. Each panel represents one branch and outer fold. Points are the five saved GA candidates, positioned by empirical development-CV regret and Jaccard distance from the highest-score mask. Shading denotes the absolute regret-eligible region $R_r\leq0.01$, and dashed lines mark $\delta=0.01$. Open circles identify highest-score candidates and diamonds identify selected regret-constrained medoids; markers overlap when the same candidate satisfies both roles. Selection and locking used outer-training data only.}
\label{fig:supp16}
\end{figure}

\begin{figure}[p]
\centering
\textit{Supplementary Figure S16 (continued).}\par\medskip
\makebox[\linewidth][c]{\includegraphics[width=170mm]{figures/figure_s25_repeat_2.pdf}}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=170mm]{figures/figure_s26.pdf}}
\caption{Candidate agreement, partition sensitivity, and locked-signature composition in TCGA GBM/LGG. a) Mean pairwise Jaccard and chance-corrected Nogueira stability among five saved candidates. Each point is the unweighted mean of five fold-level bank statistics for one repeat--branch combination and is conditional on each bank's fixed post-Direct universe. Filled circles and open squares denote repeats 1 and 2. The coefficients are shown side by side and are not connected as an effect estimate; a small negative finite-sample Nogueira estimate is valid. b) Jaccard similarity for the 20 dependent outer-fold locked-signature pairs per branch, summarized descriptively. c) Selected feature count and SCNV fraction for each of 30 locked signatures. Eleven of 526 selected-feature occurrences were SCNV features; composition largely reflects upstream Direct screening and is not evidence of biological modality preference. These summaries characterize conditional algorithmic agreement and participant-partition sensitivity, not biomarker stability.}
\label{fig:supp17}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=170mm]{figures/figure_s27.pdf}}
\caption{Repeated internal out-of-fold comparator audit in TCGA GBM/LGG. a) Mean repeated-OOF macro one-vs-rest AUROC for Direct, RFECV-only, highest-score locking, legacy top-three medoid, unrestricted medoid, and regret-constrained medoid selection. b) Paired regret-constrained-medoid-minus-comparator AUROC differences. Points in a) and b) have 95\% participant-clustered, class-stratified bootstrap intervals from 2,000 resamples retaining both repeat-specific predictions for each sampled participant. c) Mean feature count across ten outer folds. The protocol capped each post-Direct universe at 100 features. Colors identify branches. All three regret-medoid-versus-RFECV-only intervals included zero; this internal analysis does not demonstrate equivalence or global predictive superiority.}
\label{fig:supp18}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=170mm]{figures/figure_s20.pdf}}
\caption{Private MGMT radiomics internal demonstration using regret-constrained locking. a) Cohort reduction, validated MGMT linkage, fixed development/held-out split, and the full and perturbation-filtered feature spaces. b) Feature counts through Direct screening, RFECV target estimation, five completed GA runs, and strict regret-constrained locking. c) Mean pairwise Jaccard and chance-corrected Nogueira agreement across five seeds. d) Paired held-out AUROC differences for locked-minus-Direct and perturbation-filtered-minus-full locked signatures, with 95\% class-stratified bootstrap intervals. Both feature spaces use the same 197 participants and split; the filtered-space contrast is a label-independent feature-definition sensitivity analysis, not external validation. All paired intervals include zero. Five-seed summaries do not estimate participant-resampling or biomarker stability, and automatic tumor-core regions were not manually adjudicated.}
\label{fig:supp19}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=170mm]{figures/figure_s9.pdf}}
\caption{Secondary tuned-random-forest sensitivity. a) Nested development-CV AUROC for the three branches, with the prespecified primary branch identified. b) Held-out tuned-random-forest AUROC, AUPRC, and balanced accuracy with uncertainty intervals. c) Random-forest importance values for the primary SVM-L1 signature. This analysis is descriptive and is not used to support a core claim.}
\label{fig:supp20}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=180mm]{figures/figure_s10.png}}
\caption{CGGA computational characterization. a) Elapsed time for preprocessing, initial selection, RFECV, and GA search by branch on a logarithmic scale. b) Total runtime by branch. Complete runtime and peak-memory values are retained in the machine-readable computational audit.}
\label{fig:supp21}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=180mm]{figures/figure_s11.png}}
\caption{Exploratory CGGA comparators. a) Size-matched random-subset null distributions with the WrapEvoFS and Elastic Net estimates overlaid. b) Matched-size held-out AUROC, AUPRC, and balanced accuracy for WrapEvoFS and Elastic Net. c) WrapEvoFS-minus-Elastic Net differences for the same three metrics. One-sided empirical random-subset tail probabilities were not multiplicity adjusted, and all paired locked-medoid-minus-Elastic Net intervals include zero.}
\label{fig:supp22}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=180mm,height=0.78\textheight,keepaspectratio]{figures/figure_s4.png}}
\caption{Full-feature Bayesian predictive reference. a) One-vs-rest ROC and precision-recall curves together with the row-normalized confusion matrix. b) Exploratory posterior coefficient rankings for the three dementia contrasts; points show posterior means and intervals show 95\% credible intervals. c) One-vs-rest calibration, confidence and prediction-margin distributions, and posterior probability assigned to the true class. d) A representative four-chain coefficient trace, the empirical distribution of split R-hat values, and bulk effective sample sizes. Posterior probability rankings retained discrimination while coefficient-level intervals reflected diffuse high-dimensional uncertainty.}
\label{fig:supp23}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=180mm]{figures/figure_s6.png}}
\caption{Restricted Bayesian sampling diagnostics for the development-derived 81-candidate model. a) Representative four-chain trace for the PPP3CA|PPP3R1 dementia-versus-MCI coefficient. b) Rank-normalized R-hat distributions for the three contrasts, with the dashed line at 1.01. c) Bulk effective-sample-size distributions for the three contrasts.}
\label{fig:supp24}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=180mm]{figures/figure_s5.png}}
\caption{Training-only STABL analysis. a) Proteomic and b) metabolomic stability paths across inverse regularization strength; colored paths are selected features and dashed lines are selection thresholds. c) Maximum selection-frequency distributions for selected and background features in each omic block. d) Maximum stability for the eight STABL-selected features; asterisks identify features shared with the WrapEvoFS locked-signature union.}
\label{fig:supp25}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=180mm]{figures/figure_s14.png}}
\caption{Integrated candidate-restricted Bayesian/STABL/BLiP analysis. a) Training-derived construction of the 81-candidate restricted set from the 78-feature WrapEvoFS union and eight STABL selections with five shared candidates. b) Restricted-model posterior coefficient summaries for the five shared candidates across three dementia contrasts. c) Held-out balanced accuracy, macro F1, and macro one-vs-rest AUROC for the full Bayesian reference, restricted Laplace Bayesian model, candidate-union random forest, and STABL random forest. d) BLiP selection counts under the primary and sensitivity settings. Primary BLiP retained 81 candidates; stricter-FDR and larger-effect settings retained 47 and 32.}
\label{fig:supp26}
\end{figure}

\begin{figure}[p]
\centering
\makebox[\linewidth][c]{\includegraphics[width=170mm]{figures/figure_s24.pdf}}
\caption{Controlled synthetic candidate-bank locking simulation. a) Primary equal-scenario mean regret-constrained-medoid-minus-highest-score hidden-oracle regret across locking-score noise ratios and candidate topologies; shaded bands show the interquartile range across nine heterogeneity--proxy combinations. Negative differences favor the regret-constrained medoid. b) Matched 27-scenario tolerance sensitivity showing mean empirical regret against mean full-bank Jaccard; labels identify $\delta$, and increasing tolerance enlarged the eligible pool from 1.00 to 3.69 candidates on average. c) Matched 27-scenario regret-constrained-medoid-minus-comparator hidden-oracle regret across candidate-bank sizes; negative differences favor the regret-constrained medoid. The primary $R=5$, $\delta=0.01$ grid comprised 1,620,000 banks, and the complete sensitivity workload comprised 4,995,000. The regret-constrained medoid had no configured empirical-regret violation. Hidden utility is not AUROC; no GA or classifier was fitted.}
\label{fig:supp27}
\end{figure}

\section{Supplementary Tables}

\input{tables/table_38.tex}

\input{tables/table_40.tex}

\input{tables/table_41.tex}

\input{tables/table_42.tex}

{\footnotesize\noindent\textit{Note.} The local raw-to-analysis bundle contained 529 common frontal-proteomic/metabolomic profiles; two participants with unavailable or unrecognized bScore were excluded. Age values recorded as 90+ were represented by the lower-bound value 90 for median and IQR summaries. APOE4 percentages use participants with known yes/no status; PMI availability is reported in the final column.\par}

\input{tables/table_37.tex}

\paragraph*{A. Five-run locking audit}

\input{tables/table_07.tex}

\paragraph*{B. Locked-medoid agreement summary}

\input{tables/table_08.tex}

\paragraph*{Panel A}

\input{tables/table_09.tex}

\paragraph*{Panel B}

\input{tables/table_10.tex}

\paragraph*{Panel C}

\input{tables/table_11.tex}

\paragraph*{Panel D}

\input{tables/table_12.tex}

\input{tables/table_29.tex}

\paragraph*{Panel B. Paired-interval audit}

\input{tables/table_30.tex}

\paragraph*{Panel C. Target fidelity and success counts}

\input{tables/table_31.tex}

\paragraph*{Panel D. Locked-signature cross-center recurrence}

\input{tables/table_32.tex}

\paragraph*{A. Locked-medoid point estimates}

\input{tables/table_33.tex}

\paragraph*{B. Paired locked-medoid-minus-Direct differences}

\input{tables/table_34.tex}

{\footnotesize\noindent\textit{Note.} Low, Moderate, and High were encoded 0, 1, and 2. Severe errors are direct Low-to-High or High-to-Low classifications. Intervals use 2,000 common resamples stratified by held-out center and true class (seed 42). Negative MAE and severe-error differences favor the locked signature.\par}

\paragraph*{A. Center-level composition and demographics}

\input{tables/table_35.tex}

\paragraph*{B. Source-to-analysis bScore mapping}

\input{tables/table_36.tex}

\input{tables/table_44.tex}

{\footnotesize\noindent\textit{Note.} All rules in the following retrospective locking comparison used only the five saved development-run candidates. The highest-development-CV rule ignores overlap; the all-five medoid expands the pool from the recorded top three to all five. CV regret is the best saved development-CV macro-AUROC minus the recorded legacy top-three-medoid value. Detailed condition-level records are supplied as a machine-readable companion.\par}

\input{tables/table_21.tex}

\input{tables/table_22.tex}

\input{tables/table_23.tex}

\paragraph*{A. Matched-size point estimates}

\input{tables/table_24.tex}

\paragraph*{B. Random-subset tail summaries}

\input{tables/table_25.tex}

\paragraph*{C. Paired locked-medoid-minus-Elastic Net differences}

\input{tables/table_26.tex}

\input{tables/table_45.tex}

\input{tables/table_46.tex}

\input{tables/table_43.tex}

\paragraph*{Panel A}

\input{tables/table_13.tex}

\paragraph*{Panel B}

\input{tables/table_14.tex}

\paragraph*{Panel C}

\input{tables/table_15.tex}

\paragraph*{A. Cross-method overlap}

\input{tables/table_16.tex}

\paragraph*{B. Restricted posterior summaries}

\input{tables/table_17.tex}

\paragraph*{C. Restricted-model diagnostics}

\input{tables/table_18.tex}

\paragraph*{D. Held-out summaries}

\input{tables/table_19.tex}

\paragraph*{E. BLiP settings}

\input{tables/table_20.tex}

\paragraph*{A. Overall performance}

\input{tables/table_27.tex}

\paragraph*{B. Class-specific performance}

\input{tables/table_28.tex}

\input{tables/table_47.tex}

\FloatBarrier

\input{tables/table_48.tex}

\input{tables/table_39.tex}

\input{tables/table_05.tex}

\input{tables/table_06.tex}
